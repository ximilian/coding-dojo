export default class fizzbuzz {
    count(a: int) {
        var result = 0
        for (var i = 0; i < a.length; ++i) {
            result += a[i] !== b[i] ? 1 : 0
        }
        return result
    }
}
